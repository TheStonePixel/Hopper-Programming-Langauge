// Linux process/system calls — delegates to the x86_64 target.
//
// exit(), fork(), execve(), waitpid() are provided by x86_64/sys.hop.
// Swap the import below to retarget to a different ISA.

import sys from x86_64
