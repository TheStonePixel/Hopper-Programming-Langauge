// EXPECT: 3 3 3 -1

import io   from linux
import math from math

extern function printf(String fmt, ...) int

entry main(int argc, address argv) {
    printf("%d %d %d %d\n", min(3, 5), min(5, 3), min(3, 3), min(-1, 1))
}
