// Hopper self-hosted compiler
// This is the bootstrapping entry point.
// Currently a placeholder — the JS compiler in compiler/ must be complete
// enough to compile this file before self-hosting is possible.

import "stdlib/io"

function main() int {
    print("hopper compiler\n", 16)
    return 0
}
