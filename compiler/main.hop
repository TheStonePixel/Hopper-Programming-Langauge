// compiler/main.hop — self-hosted Hopper compiler entry point
//
// Compiled by kindling (the JS bootstrap) to produce build/hopper.
// Replaces: node kindling/hopper build ...
//
// Phase 1: reads a JSON AST produced by kindling/src/parse.js on stdin,
//          walks it, and emits LLVM IR to stdout.

entry main {
    // TODO: read JSON AST from stdin, walk it, emit LLVM IR
}
