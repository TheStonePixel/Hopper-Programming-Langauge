// hcc.hop — Hopper Compiler entry point
//
// Usage:
//   hcc -c <file.hop> [-o <out>] [-L<dir>] [-l<lib>] [-O2]

import json    from compiler.json
import ast     from compiler.ast
import codegen from compiler.codegen

function parseArgs(int argc, address argv)
function readStdin() string
function compile(string json) string
function emit(string ir, string outPath)

entry main(int argc, address argv) {
}
