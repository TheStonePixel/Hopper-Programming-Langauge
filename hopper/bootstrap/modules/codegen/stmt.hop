// stmt.hop — statement codegen
//
// VarDecl, Assign, If, While, For, Return, Break, Continue, etc.

function genStatement(address node)
function genVarDecl(address node)
function genAssign(address node)
function genIf(address node)
function genWhile(address node)
function genFor(address node)
function genReturn(address node)
function genBreak()
function genContinue()
