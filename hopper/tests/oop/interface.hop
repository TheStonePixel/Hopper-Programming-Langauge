// Test compile-time interface conformance checking.
// The ascii String class implements IString — verified at compile time.
// EXPECT: ok
import ascii from string
import io from linux

entry main {
    String s = String(4)
    s.append(111)
    s.append(107)
    s.append(10)
    write(1, s.pointer(), s.length())
}
