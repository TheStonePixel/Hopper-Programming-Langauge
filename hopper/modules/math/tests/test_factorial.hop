// EXPECT: 1 1 2 6 120 3628800

import io   from linux
import math from math

extern function printf(String fmt, ...) int

entry main(int argc, address argv) {
    printf("%d %d %d %d %d %d\n",
        factorial(0),
        factorial(1),
        factorial(2),
        factorial(3),
        factorial(5),
        factorial(10))
}
