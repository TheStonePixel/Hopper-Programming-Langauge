{
  "name": "regex",
  "version": "0.1.0",
  "description": "Regex pattern matching: match and matchAt with ., *, ?, +, [], ^ anchors",
  "license": "MIT",
  "type": "library",
  "dependencies": {},
  "exports": {
    "Regex": {
      "interface":      "interfaces/Regex.hop",
      "implementation": "src/Regex.hop"
    }
  }
}
