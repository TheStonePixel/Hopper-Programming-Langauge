contract Concurrent { }

contract Mutex {
    function lock()
    function tryLock() bool
    function unlock()
    function rawHandle() address
}

contract Condvar {
    function wait(address mutHandle)
    function signal()
    function broadcast()
    function rawHandle() address
}
