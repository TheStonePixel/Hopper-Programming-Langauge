{
  "name": "regex",
  "version": "0.1.0",
  "description": "Regex pattern matching: match and matchAt with ., *, ?, +, [], ^ anchors",
  "license": "MIT",
  "type": "library",
  "dependencies": {},
  "exports": {
    "Regex": {
      "for":      "interfaces/Regex.hop",
      "use": "src/Regex.hop"
    }
  },
  "targets": {
    "host": {
      "Regex": {
        "for":      "interfaces/Regex.hop",
        "use": "src/Regex.hop"
      }
    }
  }
}
