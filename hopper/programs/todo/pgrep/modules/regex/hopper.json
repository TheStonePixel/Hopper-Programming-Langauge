{
  "name": "regex",
  "version": "0.1.0",
  "description": "Regex pattern matching: match and matchAt with ., *, ?, +, [], ^ anchors",
  "license": "MIT",
  "type": "library",
  "dependencies": {},
  "exports": {
    "Regex": {
      "for": "interfaces/Regex.hop",
      "use": "src/Regex.hop"
    }
  },
  "use": {
    "Regex": {
      "interface": "interfaces/Regex.hop",
      "implementation": {
        "x86_64": "src/Regex.hop"
      }
    }
  }
}
