// smoke test — verifies the program builds and basic proc parsing works
import io from linux

extern function printf(String fmt, ...) int

entry main(int argc, address argv) {
    printf("htop: smoke test passed\n")
}
