// Tests linux/fs.hop — open and close
// EXPECT: 1

import fs from linux

extern function printf(String fmt, ...) int

entry main {
    int fd = open("/dev/null", O_WRONLY, 0)
    int ok = 0
    if (fd >= 0) {
        ok = 1
        close(fd)
    }
    printf("%d\n", ok)
}
