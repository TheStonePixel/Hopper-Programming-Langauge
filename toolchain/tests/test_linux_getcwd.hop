// Tests linux/fs.hop — getcwd syscall
// EXPECT: ok

import fs from linux
import io from linux

entry main {
    address buf = allocate 256
    int r = getcwd(buf, 256)
    if (r > 0) {
        write(1, "ok\n", 3)
    }
    deallocate buf
}
