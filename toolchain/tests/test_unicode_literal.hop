// EXPECT: Ω
// EXPECT: 😀
import String from utf8
import io from linux

entry main {
    String s = String(16)
    s.append(U+03A9)
    s.append('\n')
    write(1, s.pointer(), s.length())
    s.clear()

    s.append(U+1F600)
    s.append('\n')
    write(1, s.pointer(), s.length())
}
