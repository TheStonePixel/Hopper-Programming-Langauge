{
  "name": "CLI",
  "version": "0.1.0",
  "description": "A unified standalone command-line toolchain drives the entire development lifecycle of your code—from project initialization to dependency management, compilation, and diagnostics.",
  "license": "Apache 2.0",
  "entry": "main.hop",
  "scripts": {
    "build": "hopper build",
    "run": "hopper run",
    "test": "hopper test",
    "check": "hopper check",
    "format": "hopper format -w main.hop"
  },
  "dependencies": {}
}
